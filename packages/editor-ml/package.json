{
  "name": "@pixflow/editor-ml",
  "version": "0.0.0",
  "private": true,
  "description": "Pixflow editor ML subsystem — same-origin model loader with SHA-256 integrity (PR #9). Face detection lands in PR #10.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./loader": "./src/loader/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
